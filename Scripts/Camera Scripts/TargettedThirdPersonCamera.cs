using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TargettedThirdPersonCamera : CameraImpl
{
    public override void refresh(Vector2 newInfluence)
    {
        base.refresh(newInfluence);
    }
}
